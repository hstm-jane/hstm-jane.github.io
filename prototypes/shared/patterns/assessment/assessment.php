<?php

/*  =============================================================================
    Pattern: Assessment
    ========================================================================== */

    // trying to decide if I should write an assessment class or break it into three classes -- rating, actor, statement

    class Assessment extends Pattern
    {
        protected $settings = array(

        );

    }

/*  =============================================================================
    Helper Function: Create a temporary Assessment instance and return html
    ========================================================================== */

    // function createAssessment($options = array()) {
    //     $html = $slat->html();
    //     return $html;
    // }
